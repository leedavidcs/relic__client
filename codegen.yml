schema: http://localhost:3031/graphql
documents:
  - src/**/*.{gql,graphql}
generates:
  ./src/typings-graphql.d.ts:
    plugins:
      - typescript-graphql-files-modules
  ./src/graphql/types/custom-types.ts:
    plugins:
      - typescript
